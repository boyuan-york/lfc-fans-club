# Assets

Images currently load from Unsplash CDN URLs in HTML. For offline use or faster loads:

1. Download royalty-free stadium/fan photos  
2. Save as `hero.jpg`, `anfield.jpg`, etc.  
3. Update `src` paths in the HTML files  

Always include descriptive `alt` text (already set in markup).
